# Docs examples

Do not use TypeScript typings in example source (`Example.tsx`, `Simple.tsx`, etc.). The live interpreter crashes on annotations, interfaces, type aliases, `as` casts, and generics.

Write untyped JS-friendly code. See `Button/Simple.tsx` and `TextShimmer/Example.tsx`.

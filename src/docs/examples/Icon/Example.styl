@import '~theme'

.root
  width calc(100% + 16px)
  max-height 100%

  @media(max-width: $tablet)
    max-height 40vh

.item
  display inline-flex
  align-items center
  padding var(--indent-s)
  width 10em

  & > svg
    flex-shrink 0
    margin-right var(--indent-s)


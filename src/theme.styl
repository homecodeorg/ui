$tablet = 700px
$desktop = 1200px

borderRadius(mod = 1)
  border-radius mod * 4px

shadow()
  box-shadow 0 10px 20px var(--accent-color)

outline(color = var(--accent-color), size = 2px, rest...)
  if color
    box-shadow inset 0 0 0 size color rest
  else
    box-shadow none rest

control()
  appearance none
  outline none
  box-sizing border-box
  transition .2s ease-out
  transition-property background-color, box-shadow, opacity

  &:hover
  &:focus
    background-color var(--active-color-alpha-100)

  &[disabled]
    opacity .4
    background-color var(--accent-color-alpha-100)
    pointer-events none

controlButtonVariant(variant)
  if variant == 'clear' or variant == 'text'
    background-color transparent

  if variant == 'default'
    background-color var(--accent-color-alpha-100)

  if variant == 'outlined'
    background-color transparent
    outline(var(--accent-color))

  if variant == 'primary'
    --active-button-color #000
    background-color var(--active-color)
    color var(--active-button-color)

controlButtonVariantHover(variant)
  if variant == 'text'
    color var(--active-color)

  if variant == 'clear'
    background-color var(--active-color-alpha-300)

  if variant == 'default'
    background-color var(--active-color-alpha-300)

  if variant == 'primary'
    background-color var(--active-color-alpha-800)

controlButtonVariantFocus(variant)
  if variant == 'text'
    color var(--active-color)
    background-color transparent

  if variant == 'clear'
    background-color var(--active-color-alpha-300)

  if variant == 'default'
    background-color var(--active-color-alpha-300)

  if variant == 'outlined'
    background-color var(--active-color-alpha-300)
    outline(var(--active-color))

  if variant == 'primary'
    background-color var(--active-color)
    outline(var(--accent-color))


controlFocused(args...)
  outline(var(--active-color), 2px, args)

controlDisabled()
  outline(var(--accent-color))
  color var(--accent-color)
  cursor not-allowed
  opacity .5

$controlSizeS = 30px
$controlSizeM = 40px
$controlSizeL = 50px
$controlSizeXL = 60px

controlSize(size)
  if size == 's'
    min-height $controlSizeS
    min-width $controlSizeS
    borderRadiusSize('s')

  if size == 'm'
    min-height $controlSizeM
    min-width $controlSizeM
    borderRadiusSize('m')

  if size == 'l'
    min-height $controlSizeL
    min-width $controlSizeL
    borderRadiusSize('l')

  if size == 'xl'
    min-height $controlSizeXL
    min-width $controlSizeXL
    borderRadiusSize('xl')

controlFontSize(size)
  if size == 's'
    font-size 12px
  if size == 'm'
    font-size 16px
  if size == 'l'
    font-size 20px
  if size == 'xl'
    font-size 24px

$controlPaddingS = 14px
$controlPaddingM = 16px
$controlPaddingL = 18px
$controlPaddingXL = 20px

controlPaddingSize(size)
  if size == 's'
    padding '0 %s' % $controlPaddingS
  if size == 'm'
    padding '0 %s' % $controlPaddingM
  if size == 'l'
    padding '0 %s' % $controlPaddingL
  if size == 'xl'
    padding '0 %s' % $controlPaddingXL

$borderRadiusS = 4px
$borderRadiusM = 6px
$borderRadiusL = 8px
$borderRadiusXL = 10px

borderRadiusSize(size)
  if size == 's'
    border-radius $borderRadiusS
  if size == 'm'
    border-radius $borderRadiusM
  if size == 'l'
    border-radius $borderRadiusL
  if size == 'xl'
    border-radius $borderRadiusXL

borderRadiusRoundSize(size)
  if size == 's'
    border-radius 'calc(%s / 2)' % $controlSizeS
  if size == 'm'
    border-radius 'calc(%s / 2)' % $controlSizeM
  if size == 'l'
    border-radius 'calc(%s / 2)' % $controlSizeL
  if size == 'xl'
    border-radius 'calc(%s / 2)' % $controlSizeXL

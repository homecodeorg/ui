import { FormattedText } from '../FormattedText/FormattedText';
import type { ChatFormattedTextProps } from './types';

export function ChatFormattedText(props: ChatFormattedTextProps) {
  return <FormattedText {...props} />;
}
